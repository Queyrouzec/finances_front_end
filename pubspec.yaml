dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_mobx: ^2.0.6+5
  mobx: ^2.2.0
  provider: ^6.0.5
  uuid: ^3.0.7

description: A new Flutter project.

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.2.0

environment:
  sdk: '>=3.0.0'

flutter:
  uses-material-design: true

name: finances_front_end
publish_to: 'none' # Remove this line if you wish to publish to pub.devversion: 1.0.0+1